#!/bin/bash
set -e

if [ "$1" = "remove" ] || [ "$1" = "purge" ]; then
    echo "Pour désactiver complètement le service :"
    echo "  systemctl --user stop dictee"
    echo "  systemctl --user disable dictee"
fi

exit 0
