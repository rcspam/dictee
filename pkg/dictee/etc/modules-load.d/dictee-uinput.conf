# Auto-load uinput at boot — required for dotool / dictee-ptt to inject
# keystrokes and read keyboard events on distros that don't load it by
# default (Fedora, RHEL, openSUSE). Arch and Ubuntu auto-load it.
uinput
