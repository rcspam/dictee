# Build artifacts
/target/
# Downloaded model weights (~2.5 GB) — fetch via the curl commands in the README/script
/nemotron_multi/
# Local test audio (may contain personal/meeting recordings)
*.wav
# Transcription outputs — contain personal/meeting content, never commit
*.txt
# Python bytecode cache
__pycache__/
